{
  "name": "ads-report-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute ads-monitor-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute ads-monitor-db --local --file=./schema.sql"
  },
  "dependencies": {
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
